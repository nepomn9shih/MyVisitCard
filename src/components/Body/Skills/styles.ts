import styled from "styled-components";

export const StyledSkillsBlock = styled.section`
  min-height: 476px;
  background-color: var(--main-color);
  border-radius: 10px;
  padding: 10px 10px 20px 10px;
  box-shadow: var(--box-shadow);
`;
